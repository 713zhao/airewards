import 'package:injectable/injectable.dart';

/// Core services module for dependency injection
@module
abstract class CoreServicesModule {
  // Services are registered via @Injectable annotations on the classes themselves
  // This module can contain factory methods for complex initializations if needed
}